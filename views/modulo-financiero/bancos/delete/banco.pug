script.
  const deleteAdminPanel = (id) => {
    Swal.fire({
      title: "#{translation.ALERTS.ARE_YOU_SURE}",
      text: "#{translation.ALERTS.YOU_CANT_REVERT_THIS_ACTION}", 
      icon: "warning",
      showCancelButton: true,
      confirmButtonColor: "#3085d6",
      cancelButtonColor: "#d33",
      confirmButtonText: "#{translation.ALERTS.YES_DELETE}",
    }).then((result) => {
      if (result.isConfirmed) {
        Swal.showLoading();
        fetch(`/admin-bancos/` + id, {
          method: "delete",
        })
          .then((data) => {
            Swal.close();
            if (data.status === 200) {
              if (typeof tablaAlmacenes !== 'undefined') {
                tablaAlmacenes.ajax.reload();
              }
              toastSweet('Success', '#{translation.ALERTS.DELETED}', 'success');
            } else {
              Swal.fire({
                position: "center",
                icon: "error",
                title: "#{translation.ALERTS.WRONG}",
                showConfirmButton: false,
                timer: 2500,
              });
            }
          })
          .catch(() => {
            Swal.close();
            Swal.fire({
              position: "center",
              icon: "error",
              title: "#{translation.ALERTS.WRONG}",
              showConfirmButton: false,
              timer: 2500,
            });
          });
      }
    });
  };
