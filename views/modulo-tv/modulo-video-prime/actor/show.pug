extends ../../extends/menu-superior-admin

block contenido
  .my-3
    div
      a.btn.btn-primary(href="/admin-video-prime/actores") 
        i.fas.fa-arrow-left
        span.px-3 Atras

    h1.text-center.mb-5 
      | #{data.nombre}

    .my-3.text-right 
      a.btn.btn-primary(href=`/admin-video-prime/actores/editar/${data.id}`) #{translation.EDIT}
    .card   
      .card-body 
        table.table 
          thead
            tr
              th(scope='col') #{translation.FORM.NAME}
              th(scope='col') #{translation.FORM.DESCRIPTION}
          tbody
            tr
              th(scope='row') ID
              td #{data.id}
            tr
              th(scope='row') #{translation.FORM.NAME}
              td #{data.nombre}
            tr
              th(scope='row') #{translation.FORM.NATIONALITY}
              td #{data.nacionalidad}
            tr
              th(scope='row') #{translation.FORM.BIOGRAPHY}
              td #{data.biografia}
            tr
              th(scope='row') #{translation.FORM.GENRE}
              td #{data.genero}