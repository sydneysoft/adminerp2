extends ../../extends/menu-superior-admin

block contenido
  //- h1 Reservaciones
  ul.nav.nav-tabs
    li.nav-item
      a.nav-link(href='/admin-reservaciones') #{translation.RESERVATIONS}

  script.
    const currentPath = window.location.pathname
    const navLinks = $(".nav .nav-link");
    $.each(navLinks, function (index, value) {
      if(currentPath.startsWith(value.getAttribute('href'))) {
        $.each(navLinks,  (i, v) => v.classList.remove('active'))
        value.classList.toggle('active')
      }
    });
   
