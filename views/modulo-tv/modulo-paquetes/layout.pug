extends ../../extends/menu-superior-admin

block contenido
  ul.nav.nav-tabs
    li.nav-item
      a.nav-link(href='/admin-paquetes' ) #{translation.PACKAGES}
    li.nav-item 
      a.nav-link(href='/admin-paquetes/caracteristicas' ) #{translation.CHARACTERISITICS}
  script.
    const currentPath = window.location.pathname
    const navLinks = $(".nav .nav-link");
    $.each(navLinks, function (index, value) {
      if(currentPath == value.getAttribute('href')) {
        $.each(navLinks,  (i, v) => v.classList.remove('active'))
        value.classList.toggle('active')
      }
    });
   
