script.
  function eliminarTratamientoAdmin (val) {
    Swal.fire({
      title: "#{translation.CONFIRM_DELETE}",
      text: "#{translation.CANNOT_REVERT}",
      icon: "warning",
      showCancelButton: true,
      confirmButtonColor: "#3085d6",
      cancelButtonColor: "#d33",
      confirmButtonText: "#{translation.YES_DELETE} ",
    }).then((result) => {
      if (result.isConfirmed) {
        handleDeleteTratamiento(val);
      }
    });
  }
  function handleDeleteTratamiento(val) {
    Swal.showLoading();
    $.ajax({
      method: "DELETE",
      url: `/admin-tratamientos/${val}`,
      dataType: "json",
      success: function (data) {
        console.log(data);
        Swal.close();
        if (data.ok) {
          toastSweet('Success', '#{translation.TREATMENT_DELETED}', 'success');
          tablaTratamientos.ajax.reload();
        } else {
          toastSweet('Error', '#{translation.WRONG}', 'error');
        }
      },
      error: function () {
        Swal.close();
        toastSweet('Error', '#{translation.WRONG}', 'error');
      },
    });
  }
