//- Modal para crear cita
.modal.fade.justify-content-center.align-items-center(tabindex="-1" role="dialog" id="nuevaCita")
  .modal-dialog.modal-lg
    .modal-content.modalContent
      .modal-header 
        .row(style="margin:0;")
          .col-md-12
            h3(style="margin:0px 0;") #{translation.ADD}

      .modal-body.borderBottom
        .row
          .col-md-2.col-xs-2
            div.text-center
              img(src="http://ssl.gstatic.com/s2/oz/images/sge/grey_silhouette.png" style="width:47px;border-radius:50%;")
          .col-md-10.col-xs-10
            .row
              .col-md-6
                .form-group
                  label(for="doctor") Doctor
                  select#lista-medicos
                  //- select#doctor.form-control(name="doctor")
                    option(value="0" selected) -#{translation.DOCTOR}-
                    if Array.isArray(medicos) && medicos.length > 0
                      each item in medicos
                        option(value=`${item.id}`) #{item.nombre} #{item.apellido}
                    else
                      option(value="0") -#{translation.NO_DOCTORS}- 

              .col-md-6
                .form-group
                  label(for="sedes") Sedes
                  select#lista-sedes
                  //- select#sedes.form-control(name="sedes")
                    option(value="0" selected) -#{translation.BRANCH}-
                    if Array.isArray(sedes) && sedes.length > 0
                      each item in sedes
                        option(value=`${item.id}`) #{item.nombre} - #{item.direccion}
                    else
                      option(value="0") -#{translation.NO_BRANCHES}-

        .row
          .col-12.col-md-4
            .form-group
              label(for="fecha") #{translation.DATE}
              input#fecha.form-control(type="date" placeholder=translation.DATE)
          .col-12.col-md-4
            .form-group
              label(for="estado") #{translation.STATUS} 
              select#estado.form-control
                option(value="pendiente" selected) #{translation.PENDING}
                option(value="confirmada") #{translation.CONFIRMED}
                option(value="cancelada") #{translation.CANCELED}
                option(value="atendida") #{translation.ATTENDED}

          .col-12.col-md-4
            .form-group
              label(for="tipoCita") #{translation.APPOINTMENT_TYPE}
              select#tipoCita.form-control
                option(value="virtual") #{translation.VIRTUAL}
                option(value="presencial") #{translation.FACE_TO_FACE}

        .row
          .col-12.col-md-2
            .form-group
              label(for="hora") #{translation.START_TIME}
              input#hora.form-control(type="time")
          .col-12.col-md-2
            .form-group
              label(for="colorpicker") #{translation.COLOR}
              input#colorpicker.form-control(type="color" value="#0000ff")
              input#idCita(type="text" hidden)
          .col-12.col-md-4
            .form-group
              label(for="precio") #{translation.PRICE} 
              input#precio.form-control(type="number" placeholder=translation.PRICE)
          .col-12.col-md-4 
            .form-group 
              label(for="especialidad") #{translation.SPECIALTY} 
              select#lista-especialidades(name="especialidades") 
              //- select#especialidad.form-control
                if Array.isArray(especialidades) && especialidades.length > 0
                  each item in especialidades
                    option(value=`${item.id}`) #{item.nombre}
                else
                  option(value="0") -#{translation.NO_SPECIALTIES}-
        .row 
          .col-12 
            .form-group 
              label(for="paciente") #{translation.PATIENT} 
              select#lista-pacientes(name="pacientes") 
              //- select#paciente.form-control
                option(value="0") -#{translation.SELECT_PATIENT}-
                if Array.isArray(pacientes) && pacientes.length > 0
                  each item in pacientes
                    option(value=`${item.id}`) #{item.nombre} #{item.apellido_paterno} #{item.apellido_materno}
                else
                  option(value="0") -#{translation.NO_PATIENTS}-
          .col-12
            .form-group
              label(for="nota") #{translation.NOTE} 
              textarea#nota.form-control(name="" cols="30" rows="10" placeholder=translation.NOTE)  

      .modal-footer(style="padding:8px;")
        .row(style="margin:0;")
          .col-md-12.text-right
            button#addNewCita.btn.btn-success.nuevo(type="button" style="margin-left:7px;margin-right:7px;") #{translation.ADD}
              i.fas.fa-save.ml-2
            button.btn.btn-danger(type="buton" data-dismiss="modal") #{translation.CLOSE}
              i.fas.fa-times.ml-2
