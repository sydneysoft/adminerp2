.modal.fade.justify-content-center.align-items-center(tabindex="-1" role="dialog" id="editarMedicoModal")
  .modal-dialog.modal-lg
    .modal-content.modalContent(style="height: 100%;")
      .modal-header 
        .row(style="margin:0;")
          .col-md-12
            h3(style="margin:0px 0;") #{translation.EDIT_MEDIC}

      .modal-body.borderBottom

        .row
          .col-12.col-md-3
            .form-group
              label(for="first_nameEditar") #{translation.FIRST_NAME} *
              input#first_nameEditar.form-control(type="text" placeholder=translation.FIRST_NAME)
          .col-12.col-md-3
            .form-group
              label(for="middle_nameEditar") #{translation.MIDDLE_NAME}
              input#middle_nameEditar.form-control(type="text" placeholder=translation.MIDDLE_NAME)
          .col-12.col-md-6
            .form-group
              label(for="surnamesEditar") #{translation.SURNAMES} *
              input#surnamesEditar.form-control(type="text" placeholder=translation.SURNAMES)
        .row 
          .col-12.col-md-4
            .form-group 
              label(for="especialidadEditar") #{translation.SPECIALTY} *
              select#lista-especialidades-editar
              
              //- input#especialidadEditar.form-control(type="text" placeholder=translation.SPECIALTY)
              //- select#especialidadEditar.form-control(name="especialidad")
              //-   if Array.isArray(especialidades) && especialidades.length > 0
              //-     each especialidad in especialidades
              //-       option(value=especialidad.id) #{especialidad.nombre}
              //-   else
              //-     option(value="0") #{translation.NO_SPECIALTY} 
          
          .col-12.col-md-4
            .form-group 
              label(for="tipo_documentoEditar") #{translation.DOCUMENT_TYPE} *
              select#lista-documentos-editar
              //- select#tipo_documentoEditar.form-control
              //-   if Array.isArray(tipo_documento) && tipo_documento.length === 1
              //-     each tipo in tipo_documento[0].valor
              //-       option(value=tipo)
              //-         span.text-capitalize #{tipo}
              //-   else
              //-     option(value="0") #{translation.NO_DOCUMENT_TYPE}
              //- input#tipo_documentoEditar.form-control(type="text" placeholder="Tipo de Documento")
          .col-12.col-md-4
            .form-group 
              label(for="numero_documentoEditar") #{translation.DOCUMENT_NUMBER} *
              input#numero_documentoEditar.form-control(type="text" placeholder=translation.DOCUMENT_NUMBER)

        .row
          .col-12.col-md-4
            .form-group 
              label(for="celularEditar") #{translation.CELULAR} *
              input#celularEditar.form-control(type="text" placeholder=translation.CELULAR)
          .col-12.col-md-4
            .form-group 
              label(for="correoEditar") #{translation.EMAIL}
              input#correoEditar.form-control(type="email" placeholder=translation.EMAIL)
          .col-12.col-md-4 
            .form-group
              label(for="tiempo_citaEditar") #{translation.APPOINTMENT_TIME} (#{translation.MINUTES})
              input#tiempo_citaEditar.form-control(type="number" placeholder=`0 (${DEFAULT_TIME_MESSAGE})`)

        .row.align-items-end
          .col-6.col-md-3
            .form-group
              label(for="dia_de_editar") #{translation.FROM}
              select#dia_de_editar.form-control
                option(value="lunes") #{translation.DAYS.MONDAY}
                option(value="martes") #{translation.DAYS.TUESDAY}
                option(value="miercoles") #{translation.DAYS.WEDNESDAY}
                option(value="jueves") #{translation.DAYS.THURSDAY}
                option(value="viernes") #{translation.DAYS.FRIDAY}
                option(value="sabado") #{translation.DAYS.SATURDAY}
                option(value="domingo") #{translation.DAYS.SUNDAY}
          .col-6.col-md-3
            .form-group
              label(for="dia_a_editar") #{translation.TO}
              select#dia_a_editar.form-control
                option(value="lunes") #{translation.DAYS.MONDAY}
                option(value="martes") #{translation.DAYS.TUESDAY}
                option(value="miercoles") #{translation.DAYS.WEDNESDAY}
                option(value="jueves") #{translation.DAYS.THURSDAY}
                option(value="viernes") #{translation.DAYS.FRIDAY}
                option(value="sabado") #{translation.DAYS.SATURDAY}
                option(value="domingo") #{translation.DAYS.SUNDAY}
          .col-6.col-md-2
            .form-group 
              label(for="hora_de_editar") #{translation.FROM}
              input#hora_de_editar.form-control(type="time" placeholder="Hora de inicio")
          .col-6.col-md-2
            .form-group 
              label(for="hora_a_editar") #{translation.TO}
              input#hora_a_editar.form-control(type="time" placeholder="Hora de fin")
          .col-6.col-md-2 
            .form-group
              button#agregar_horario_editar(type="button" class="btn btn-success mt-4") #{translation.ADD}
                i.fas-fa-plus.ml-2
          .col-12
            .row.align-items-start
              .col-5
                .my-3
                  #show_horarios_editar
              .col-7
                .btn-group
                  button#upload_cvEditar.btn.btn-info(type="button" ) #{translation.UPLOAD_CV}
                    i.fas.fa-upload.ml-2
                  button.btn.btn-primary.d-flex.align-items-center
                    label.d-block(for="subir_archivoEditar" style="position: relative;") #{translation.SEARCH_FILES} 
                  input#subir_archivoEditar(type="file" text="Subir archivo" value="Subir archivo" hidden)
                  input#cvEditar.form-control(type='text' value='' disabled hidden)
                  a#linkCvEditar.btn.btn-success.disabled(href="" target="_blank" aria-disabled=true) #{translation.SEE_CV}
                #showLinkEditar

        input#idEditar(type="text" hidden )
      .modal-footer(style="padding:8px;")
        .row(style="margin:0;")
          .col-md-12.text-right
            button#updateMedico.btn.btn-success.nuevo(type="button" style="margin-left:7px;margin-right:7px;") #{translation.SAVE}
              i.fas.fa-save.ml-2
            button.btn.btn-danger(type="buton" data-dismiss="modal") #{translation.CLOSE}
